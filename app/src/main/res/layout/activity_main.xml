<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/MoneyGreen"
    android:backgroundTint="@color/MoneyGreen"
    tools:context=".MainActivity">


   <FrameLayout
       android:id="@+id/fragment_container"
       android:layout_width="match_parent"
       android:layout_height="match_parent"
       android:layout_above="@+id/bottom_navigation" />

    <!-- Давайте рисовать!) Раскрашываем нашу навигационную панель -->

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        app:menu="@menu/bottom_navigation"
        app:itemIconTint="@color/black"
        app:itemTextColor="@color/black" />
    <!-- android:background="?androidoid:attr/windowBackground" //под цвет верхней панели -->
    <!-- Заканчиваем окрашывание иконок и текста самостоятельно (app:itemIconTint="@color/black" + app:itemTextColor="@color/black") -->



</RelativeLayout>